POSTGRES_USER=risuai
POSTGRES_PASSWORD=change-this-password
POSTGRES_DB=risuai
RISU_PORT=6001
# Optional: override the app connection string. The bundled `postgres` service still starts unless you edit `docker-compose.yml`.
RISU_DATABASE_URL=
